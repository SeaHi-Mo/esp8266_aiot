#
# "main" pseudo-component makefile.
#
# (Uses default behaviour of compiling all source files in directory, adding 'include' to include path.)
COMPONENT_ADD_INCLUDEDIRS := app_data_model app_dynreg_mqtt app_flash app_ntp app_ota app_version
COMPONENT_SRCDIRS := app_data_model app_dynreg_mqtt app_flash app_ntp app_ota app_version